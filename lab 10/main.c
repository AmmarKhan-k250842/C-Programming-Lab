#include <stdio.h>
#include <string.h>

int main () {

    
    // Concatenating strings
   
    /*
    char dest[20] = "Good";
    char src[] = " Morning";

    strncat(dest, src, 8);

    printf("%s", dest);
    */

   

    // Comparing Strings

    /*
    char dest[20] = "hello";
    char src[] = "helo";

    int x = strncmp(dest, src, 3);

    if (x == 0)
    {
        printf("The strings are equal");
    }
    else
    {
        printf("The strings are not equal");
    }
    */



    // Taking Input

    /*
    char name[50];

    printf("Enter your name: ");
    fgets(name, sizeof(name), stdin);

    fputs(name, stdout);
    */

    return 0;
}